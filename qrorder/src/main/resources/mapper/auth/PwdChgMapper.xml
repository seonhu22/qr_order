<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="htms.QROrder.auth.repository.PwdChgMapper">

    <update id="chgPwd">
        update sys_user set
            user_password = #{pwd},
            modify_user_id = #{userId},
            modify_datetime = now()
        where 1=1
        and user_id = #{userId}
    </update>

</mapper>
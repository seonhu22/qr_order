<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="htms.QROrder.auth.repository.SignUpMapper">

    <insert id="newUser">
        insert into sys_user (
            sys_id ,
            user_id ,
            user_nm ,
            user_password ,
            email ,
            email_valid_yn ,
            init_yn ,
            password_fail_cnt ,
            delete_yn,
            insert_user_id ,
            insert_datetime,
            sys_plant_cd
        )
        values (
            #{BRNRequest.sysId},
            #{BRNRequest.userId},
            #{BRNRequest.userNm},
            #{BRNRequest.userPassword},
            #{BRNRequest.email} ,
            'N',
            'N',
            0,
            'N',
            #{userId},
            now(),
            #{BRNRequest.businessRegiNum}
        )
    </insert>

    <insert id="newEmailChk">
        insert into sys_email_valid (
            sys_id ,
            link_sys_id ,
            encode_sys_id ,
            email_valid_yn ,
            insert_user_id ,
            insert_datetime
        )
        values (
            #{BRNRequest.sysId},
            #{BRNRequest.linkSysId},
            #{BRNRequest.encodeSysId},
            'N',
            #{userId},
            now()
        )
    </insert>

    <update id="emailValid">
        update sys_email_valid set
            email_valid_yn = 'Y',
            modify_datetime = now(),
        where 1=1
        and encode_sys_id = #{encodeSysID}
    </update>

</mapper>

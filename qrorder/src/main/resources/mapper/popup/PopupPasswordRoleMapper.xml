<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="htms.QROrder.popup.repository.PopupPasswordRoleMapper">

    <select id="chkPasswordCorrect" resultType="String">
        select
            user_password
        from sys_user su
        where 1=1
        and su.sys_id = #{sysId}
        and su.delete_yn = 'N'
        and su.use_yn = 'Y'
    </select>

    <update id="changePassword">
        update sys_user set
            user_password = #{chkNewPwd}
        where 1=1
        and sys_id = #{sysId}
    </update>

</mapper>

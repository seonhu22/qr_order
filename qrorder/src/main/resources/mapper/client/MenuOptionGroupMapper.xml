<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="htms.QROrder.client.repository.MenuOptionGroupMapper">

    <select id="getMenuOptionGroup" resultType="htms.QROrder.client.dto.MenuOptionGroupResponse">
        select
            sys_id ,
            link_sys_id ,
            group_name ,
            required_yn ,
            input_type ,
            ord_no
        from store_menu_option_group smom
        where 1=1
        and smom.link_sys_id = #{masterSysId}
        and smom.delete_yn = 'N'
        order by ord_no asc
    </select>

    <insert id="newMenuOptionGroup">
        insert into store_menu_option_group (
            sys_id ,
            link_sys_id ,
            group_name ,
            required_yn ,
            input_type ,
            delete_yn ,
            ord_no ,
            use_yn ,
            insert_datetime ,
            insert_user_id ,
            sys_plant_cd
        )
        values (
            #{newItems.sysId},
            #{newItems.linkSysId},
            #{newItems.groupName},
            #{newItems.requiredYn},
            #{newItems.inputType},
            'N',
            #{newItems.ordNo},
            'N',
            now(),
            #{userId},
            #{sysPlantCd}
        )
    </insert>
    
    <update id="updateMenuOptionGroup">
        update store_menu_option_group set
            group_name = #{updateItems.groupName} ,
            required_yn = #{updateItems.requiredYn} ,
            input_type = #{updateItems.inputType} ,
            ord_no = #{updateItems.ordNo} ,
            use_yn = #{updateItems.useYn} ,
            modify_user_id = #{userId} ,
            modify_datetime = now()
        where 1=1
        and sys_id = #{updateItems.sysId}
    </update>

    <delete id="delMenuOptionGroup">
        update store_menu_option_group set
            delete_yn = 'Y',
            modify_user_id = #{userId},
            modify_datetime = now()
        where 1=1
        and sys_id in
        <foreach collection="delItems" item="item" open="(" separator="," close=")" >
            #{item.sysId}
        </foreach>
    </delete>

    <select id="duplicateChk" resultType="boolean">
        select
            count(*) > 0
        from store_menu_option_group
        where 1=1
        and group_name = #{groupName}
        and delete_yn = 'N'
    </select>

    <select id="getOldData" resultType="htms.QROrder.client.dto.MenuOptionGroupResponse">
        select
            sys_id ,
            group_name ,
            required_yn ,
            input_type ,
            use_yn ,
            ord_no
        from store_menu_option_group smm
        where 1=1
        and smm.sys_id = #{sysId}
    </select>
</mapper>

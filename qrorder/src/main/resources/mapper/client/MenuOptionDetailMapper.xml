<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="htms.QROrder.client.repository.MenuOptionDetailMapper">

    <select id="getMenuOptionDetail" resultType="htms.QROrder.client.dto.MenuOptionDetailResponse">
        select
            sys_id ,
            link_sys_id ,
            menu_option_name ,
            menu_option_price ,
            menu_description ,
            maximum_num ,
            use_yn ,
            file_ulid ,
            ord_no
        from store_menu_option_detail smd
        where 1=1
        and link_sys_id = #{groupSysId}
        and delete_yn = 'N'
        order by ord_no asc
    </select>

    <insert id="newMenuOptionDetail">
        insert into store_menu_option_detail (
            sys_id ,
            link_sys_id ,
            menu_option_name ,
            menu_option_price ,
            menu_description ,
            maximum_num ,
            use_yn ,
            ord_no ,
            delete_yn ,
            file_ulid ,
            insert_datetime ,
            insert_user_id ,
            sys_plant_cd
        )
        values
        <foreach collection="newItems" item="item" separator="," >
            (
                #{item.sysId},
                #{item.linkSysId},
                #{item.menuOptionName},
                #{item.menuOptionPrice},
                #{item.menuDescription},
                #{item.maximumNum} ,
                #{item.useYn},
                #{item.ordNo},
                'N',
                #{item.fileUlid},
                now(),
                #{userId},
                #{sysPlantCd}
            )
        </foreach>
    </insert>

    <update id="updateMenuOptionDetail">
        <foreach collection="updateItems" item="item" separator=";">
            update store_menu_option_detail set
                menu_option_name = #{item.menuOptionName} ,
                menu_option_price = #{item.menuOptionPrice} ,
                menu_description = #{item.menuDescription} ,
                maximum_num = #{item.maximumNum} ,
                use_yn = #{item.useYn} ,
                ord_no = #{item.ordNo} ,
                modify_datetime = now(),
                modify_user_id = #{userId}
            where 1=1
            and sys_id = #{item.sysId}
        </foreach>
    </update>

    <delete id="delMenuOptionDetail">
        update store_menu_option_detail set
            delete_yn = 'Y',
            modify_datetime = now(),
            modify_user_id = #{userId}
        where 1=1
        and sys_id in
        <foreach collection="delItems" item="item" open="(" separator="," close=")">
            #{item.sysId}
        </foreach>
    </delete>

    <select id="duplicateChk" resultType="boolean">
        select
            count(*) > 0
        from store_menu_option_detail
        where 1=1
        and menu_option_name in
        <foreach collection="newItems" item="item" open="(" separator="," close=")">
            #{item.menuOptionName}
        </foreach>
        and delete_yn = 'N'
    </select>

    <select id="getOldData" resultType="htms.QROrder.client.dto.MenuOptionDetailItem">
        select
            sys_id ,
            link_sys_id ,
            menu_option_name ,
            menu_option_price ,
            menu_description ,
            maximum_num ,
            use_yn ,
            file_ulid ,
            ord_no
        from store_menu_option_detail smd
        where 1=1
        and sys_id in
        <foreach collection="menuOptionDetailItems" item="item" open="(" separator="," close=")">
            #{item.sysId}
        </foreach>
    </select>

    <select id="getDuplicateData" resultType="htms.QROrder.client.dto.MenuOptionDetailItem">
        select
            sys_id ,
            link_sys_id ,
            menu_option_name ,
            menu_option_price ,
            menu_description ,
            maximum_num ,
            use_yn ,
            file_ulid ,
            ord_no
        from store_menu_option_detail smd
        where 1=1
        and menu_option_name in
        <foreach collection="menuOptionDetailItems" item="item" open="(" separator="," close=")">
            #{item.menuOptionName}
        </foreach>
        and delete_yn = 'N'
    </select>
</mapper>

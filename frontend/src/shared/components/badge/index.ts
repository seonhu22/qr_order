/**
 * @fileoverview Badge 컴포넌트 공개 API
 *
 * @description
 * - 외부에서는 이 파일만 통해 import 한다
 *
 * @example
 * import { Badge } from '@/shared/components/badge';
 */

/* 컴포넌트 */
export { Badge } from './Badge';

/* 타입 */
export type { BadgeProps, BadgeTone, BadgeSize } from './types';

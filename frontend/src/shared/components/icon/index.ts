// src/shared/components/icon/index.ts

/**
 * @fileoverview icon 계열 컴포넌트 공개 API
 *
 * @description
 * - 상태 아이콘과 이후 확장될 공용 SVG 아이콘을 한 곳에서 재내보낸다.
 *
 * @param {never} _unused 이 파일은 런타임 파라미터를 사용하지 않는 export 배럴 파일이다.
 * @example
 * import { StatusInfoIcon } from '@/shared/components/icon';
 */

export { StatusInfoIcon } from './StatusInfoIcon';
